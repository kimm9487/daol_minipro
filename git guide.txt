1. 빈폴더를 생성 -> 해당 폴더에 우클릭 bash 커맨드를 이용하여 
git clone https://github.com/kimm9487/daol_minipro.git . 

2. 해당 브런치로 경로 변경 
git checkout jaehun


3. git 경로가 main 이 아닌 jaenhun(  사용자 경로 인지 확인 필수!)
git pull origin jaehun          # ① 최신화
# → 파일 수정 작업 ←
git add .                       # ② 변경 추가
git commit -m "메시지"          # ③ 기록
git push origin jaehun          # ④ 업로드







conda env remove -n tfod

environment.yml 파일 수정 (base 이름 변경 – 필수!)
파일을 열어서 맨 위를 이렇게 바꾸세요 (텍스트 에디터로 편집):
YAMLname: tfod  # ← base → tfod 로 변경 (또는 다른 이름)

conda env create -f environment.yml
conda activate tfod
conda list  # 설치된 패키지 목록 확인

pip install fastapi uvicorn python-multipart bcrypt

uvicorn main:app --reload